<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginLeft="@dimen/margin_cardview"
    android:layout_marginRight="@dimen/margin_cardview"
    android:layout_marginTop="@dimen/margin_cardview"
    android:layout_marginBottom="@dimen/margin_cardview"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?selectableItemBackground">

    <LinearLayout
        android:id="@+id/recipe_grid_item_layout"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView android:id="@+id/iv_recipe_image"
            android:layout_width="match_parent"
            android:layout_height="@dimen/recipe_image_height"
            android:scaleType="centerCrop"
            android:contentDescription="@string/recipe_image_description"
            tools:src="@drawable/default_image"/>

        <TextView
            android:id="@+id/tv_recipe_name"
            style="@style/TextAppearance.AppCompat.Subhead"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingLeft="@dimen/padding_recipetitle"
            android:paddingTop="@dimen/padding_recipetitle"
            android:paddingRight="@dimen/padding_recipetitle"
            android:paddingBottom="@dimen/padding_recipetitle_bottom"
            android:gravity="center"
            android:textAlignment="center"
            android:textStyle="bold"
            android:textColor="@color/colorPrimaryText"
            android:ellipsize="end"
            tools:text="recipe mock text" />

    </LinearLayout>
</android.support.v7.widget.CardView>